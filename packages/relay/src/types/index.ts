export enum LoyaltyType {
    POINT,
    TOKEN,
}
export enum WithdrawStatus {
    CLOSE,
    OPEN,
}

export enum LoyaltyPaymentInputDataStatus {
    NULL,
    CREATED,
    CREATE_CONFIRMED,
    CREATE_DENIED,
    CANCELED,
    CANCEL_CONFIRMED,
    CANCEL_DENIED,
}
